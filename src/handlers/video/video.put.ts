import Handler from '../../handler'
import { Request, Response, NextFunction  } from 'express'

export class VideoPut extends Handler {
    public editById(req, res: Response, next: NextFunction): void {}
}